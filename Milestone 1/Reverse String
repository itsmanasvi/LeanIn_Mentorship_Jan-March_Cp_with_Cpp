class Solution {
public:
    void reverseString(vector<char>& s) {
        char a;
    for(int i=0, j=s.size()-1; i<s.size()/2 && j>=0 ; i++,j--)
    {
        a=s[i];
        s[i]=s[j];
        s[j]=a;
    }      
    }
};
